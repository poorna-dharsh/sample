name: Basic Workflow on Self-Hosted Runner

on:
  workflow_dispatch: 

jobs:
  basic-job:
    runs-on: self-hosted

    steps:
    - name: Print Hello
      run: echo "Hello from self-hosted runner!"

    - name: List files
      run: ls -la

    - name: Show current directory
      run: pwd
